# Copy to prod.env and fill in real values before deploying:
#   cp prod.env.example prod.env
#
# Non-secret deployment config only. Real secrets (JWT_SECRET_KEY,
# STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, SMTP_PASSWORD) live in
# .secrets/prod.env instead — see .secrets/prod.env.example.

### Host ports ###
BACKEND_PORT=8000
FRONTEND_PORT=3000

### Frontend — baked into the browser bundle at image build time, so the
### frontend image must be rebuilt whenever these change (see
### frontend/.env.local.example) ###
NEXT_PUBLIC_API_URL=https://api.bergseehome.ch
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

### Backend (see backend/.env.example) ###
APP_NAME=apartment103-backend
ENVIRONMENT=production
MONGO_DB=apartment103

# JSON array of allowed browser origins, e.g. ["https://bergseehome.ch"]
CORS_ALLOW_ORIGINS=["https://bergseehome.ch"]

JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

OTP_LENGTH=6
OTP_TTL_SECONDS=300
OTP_RESEND_COOLDOWN_SECONDS=30
OTP_MAX_ATTEMPTS=5

# Leave SMTP_HOST unset to log OTP emails to the container's stdout instead
# of sending them (not recommended for prod). SMTP_PASSWORD lives in
# .secrets/prod.env.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_USE_TLS=true
SMTP_FROM_ADDRESS=no-reply@bergseehome.ch

# Stripe publishable key only — safe to expose, matches
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY above. STRIPE_SECRET_KEY and
# STRIPE_WEBHOOK_SECRET live in .secrets/prod.env.
STRIPE_PUBLISHABLE_KEY=
